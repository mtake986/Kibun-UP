import React from "react";

const RadioTitle = () => {
  return (
    <label className="text-sm">
      Please select the quote type you want to display on the homepage.
    </label>
  );
};

export default RadioTitle;
